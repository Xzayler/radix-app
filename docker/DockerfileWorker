FROM rust:1.90-slim-bullseye AS builder

WORKDIR /worker
COPY /worker ./
RUN cargo build --release

FROM debian:bullseye-slim
WORKDIR /worker
COPY --from=builder /worker/target/release/worker /worker/worker

CMD ["./worker"]